// Note
// * Name of component must start with capital letters

// * Try to name of the file as same as component

// * If you are using vite components must be saved with '.jsx' extension

// * No need of '.jsx' in 'create-react-app' you can use '.js' also for components but naming convention must be followed

function Chai() {
    return(
        <>
            <h1>Chai Ready</h1>
        </>
    )
}

export default Chai